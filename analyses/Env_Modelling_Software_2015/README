These files were used to produce the analysis submitted to Environmental Modelling & Software  in 2015.

------------------------------------------------------------------
The code version was based on the following commit:

commit c96ec7e2e482df9b9fcf693340d83e24a886138e
Author: jfdecarolis <jdecarolis@gmail.com>
Date:   Thu Nov 12 00:14:36 2015 -0500

    Update TechInputSplit and/or TechOutputSplit in DAT files to include period index

------------------------------------------------------------------

For reference, the modeled system is represented in EMS_system.svg.

------------------------------------------------------------------

Below is a listing of the input / output data.

Database files

EMS_base.sql: SQL file used to create a base database

EMS_base.sqlite: Binary version of the base sqlite database; includes stored results

EMS_cap40.sql: SQL file used to create a version of the database that includes a carbon cap scenario requiring a 40% cut in 2015 emissions by 2050 (cap40).

EMS_cap40.sqlite: Binary version of the cap40 sqlite database; includes stored results

EMS_cap80.sql: SQL file used to create a version of the database that includes a carbon cap scenario requiring a 80% cut in 2015 emissions by 2050 (cap80).

EMS_cap80.sqlite: Binary version of the cap40 sqlite database; includes stored results

------------------------------------------------------------------
DAT files:

The files below include the specific technology sets and discount rates used to conduct the MGA analysis.

EMS_base.dat: Data file for the base case

EMS_cap40.dat: Data file for the moderate climate scenario (cap40)

EMS_cap80.dat: Data file for the aggressive climate scenario (cap80)

EMS_cap40_integer_weight.dat: Data file used to conduct the MGA analysis on the cap40 scenario using the MGA integer weighting method.

EMS_cap40_norm_weight.dat: Data file used to conduct the MGA analysis on the cap40 scenario using the MGA normalized sector weighting method.

EMS_cap40_dr001_norm_weight.dat: Data file used to conduct the MGA analysis on the cap40 scenario assuming a 0.1% discount rate and the MGA normalized sector weighting method.

EMS_cap40_dr10_norm_weight.dat: Data file used to conduct the MGA analysis on the cap40 scenario assuming a 10% discount rate and the MGA normalized sector weighting method.

EMS_cap40_norm_weight_max_solarPV.dat: Data file used to the MGA analysis on the cap40 scenario where the use of solar PV and plugin vehicles is maximized.
